<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <provider
      android:name=".RadarFlutterInitializationProvider"
      android:authorities="${applicationId}.radar_flutter_initialization_provider"
      android:exported="false" />
  </application>
</manifest>
